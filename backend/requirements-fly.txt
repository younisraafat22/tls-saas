# Slim requirements for Fly.io deployment (no Selenium / browser deps).
# The laptop worker (worker.py) handles all browser automation.

# FastAPI & Server
fastapi>=0.109.2
uvicorn[standard]>=0.27.1
python-multipart>=0.0.9
websockets>=12.0

# Database — use PostgreSQL on Fly.io
sqlalchemy[asyncio]>=2.0.27
asyncpg>=0.29.0
alembic>=1.13.1
# aiosqlite only needed locally; keep for fallback
aiosqlite>=0.19.0

# Auth
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.1.2

# Validation & Serialization
pydantic[email]>=2.6.1
pydantic-settings>=2.1.0

# HTTP
httpx>=0.27.0
requests>=2.31.0

# Scheduling (still used on laptop; imported conditionally — kept here to avoid import errors)
apscheduler>=3.10.4

# Notifications
pywebpush>=2.0.0

# Utilities
python-dotenv>=1.0.1
cryptography>=42.0.4
Pillow>=10.2.0

# NOTE: patchright, seleniumbase, SpeechRecognition, pydub, imageio-ffmpeg
# are intentionally EXCLUDED — they are only needed on the laptop worker.
slowapi==0.1.9
